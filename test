# OpenAI Configuration
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_MAX_TOKENS=4000
OPENAI_TEMPERATURE=0.1

# Application Settings
APP_NAME='Financial Data Extraction API'
VERSION=1.0.0
DEBUG=True
SECRET_KEY=dev-secret-key-change-in-production

# File Processing
TEMP_DIR=/tmp
BATCH_SIZE=100
MAX_ROWS_PER_FILE=1000000

# Logging
LOG_LEVEL=TRACE
# Storage Configuration
# Options: 'local' or 's3'
STORAGE_TYPE=local


HOST=0.0.0.0
PORT=8000

# File Upload Settings
MAX_FILE_SIZE=500
# Maximum file size in MB (increased for large files)
LARGE_FILE_THRESHOLD=100000
# Threshold for considering a file "large" (in rows)

# Preview and Sample Settings
MAX_SAMPLE_ROWS=100
# Maximum rows to show in file info samples
MAX_PREVIEW_ROWS=1000
# Maximum rows allowed in preview endpoint

# CORS Settings
ALLOWED_HOSTS=*# Use specific domains in production, e.g., http://localhost:3000,https://yourdomain.com

# Database Settings (if you add database support later)
DATABASE_URL=sqlite:///./app.db

# Logging
# Performance Settings
CHUNK_SIZE=10000
# For processing very large files in chunks (future use)
MEMORY_LIMIT_GB=4
# Memory limit warning threshold